---
globs: src/**/*.tsx
description: User requested to remove the Yandex Object Storage step-by-step
  guide block from the UI settings (AdminSettings, BrainSettings). Keep only
  input forms and action buttons.
alwaysApply: false
---

Не выводить длинные текстовые инструкции по настройке Яндекс Object Storage (шаги 1–5) в интерфейсе. Оставить только поля ввода и кнопки управления.